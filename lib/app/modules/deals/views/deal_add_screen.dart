import 'package:flutter/material.dart';

class DealAddScreen extends StatelessWidget {
  const DealAddScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
