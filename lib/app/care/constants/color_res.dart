import 'dart:ui';

class ColorRes {
  //common color
  static const black = Color(0xff000000);
  static const white = Color(0xffffffff);
  static const grey = Color(0xff555555);

  // Brand
  static const primary = Color(0xFF0066FF);

  // Backgrounds & Surfaces
  static const background = Color(0xFFF7F9FC);
  static const surface = Color(0xFFFFFFFF);

  // Status
  static const error = Color(0xFFFF3B30);
  static const success = Color(0xFF34C759);
  static const warning = Color(0xFFFFCC00);

  // Borders & Dividers
  static const border = Color(0xFFE0E0E0);
  static const divider = Color(0xFFE0E0E0);

  // Disabled & Overlay
  static const disabled = Color(0x33000000);
  static const overlay = Color(0x66000000); // 40% Black

  // Shadows
  static const shadow = Color(0x33000000); // 20% Black

  // Text
  static const textPrimary = Color(0xFF1E1E1E);
  static const textSecondary = Color(0xFF666666);
  static const textDisabled = Color(0xFFA0A0A0);
}
