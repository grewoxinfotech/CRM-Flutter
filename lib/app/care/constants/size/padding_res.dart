import 'package:flutter/material.dart';
class PaddingRes {
  static const int paddingSize = 5;

  static const EdgeInsets all0 = EdgeInsets.zero;
  static const EdgeInsets all1 = EdgeInsets.all(paddingSize * 1);
  static const EdgeInsets all2 = EdgeInsets.all(paddingSize * 2);
  static const EdgeInsets all3 = EdgeInsets.all(paddingSize * 3);
  static const EdgeInsets all4 = EdgeInsets.all(paddingSize * 4);
  static const EdgeInsets all5 = EdgeInsets.all(paddingSize * 5);
  static const EdgeInsets all6 = EdgeInsets.all(paddingSize * 6);
  static const EdgeInsets all7 = EdgeInsets.all(paddingSize * 7);
  static const EdgeInsets all8 = EdgeInsets.all(paddingSize * 8);
  static const EdgeInsets all9 = EdgeInsets.all(paddingSize * 9);
}
